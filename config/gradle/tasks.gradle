task copyConfiguration {
        doLast {
            println "Target environment: $profile"

            copy {
                from "src/main/env/$profile"
                into "src/main/resources"
                include "**/*"
            }
        }
    }
