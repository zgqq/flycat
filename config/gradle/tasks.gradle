task copyConfiguration {
        doLast {
            println "Target environment: $env"

            copy {
                from "src/main/env/$env"
                into "src/main/resources"
                include "**/*"
            }
        }
    }
