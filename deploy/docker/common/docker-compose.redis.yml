version: '3'
services:
  db:
    container_name: "db-redis"
    image: redis:6.2.7-alpine
    restart: always
    volumes:
      - ~/deploy/data/redis:/data
    command: redis-server --save 180 1 --appendonly yes --loglevel warning --requirepass ${REDIS_PASSWORD}
    networks:
      - redis
    ports:
      - '${REDIS_PORT}:6379'
networks:
  redis:
    external:
      name: db_redis
